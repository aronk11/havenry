/**
 * Single place for anything that changes when the project moves.
 *
 * If someone forks Havenry and runs their own build, AGPL §13 requires the
 * running instance to point at *their* source. Keeping the URL here means one
 * edit, not a search across the site.
 */
export const REPO_URL = "https://github.com/aronk11/havenry";
export const ADR_URL = `${REPO_URL}/tree/main/docs/adr`;
export const LICENSE = "AGPL-3.0";
